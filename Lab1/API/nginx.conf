events {}

http {
    upstream grpc_backend {
        server api-1:8080;
        server api-2:8080;
    }

    server {
        listen 5000 http2;

        location / {
            grpc_pass grpc://grpc_backend;
        }
    }
}
